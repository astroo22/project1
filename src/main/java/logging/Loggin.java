package logging;
import org.apache.log4j.Logger;

public class Loggin {
	final static Logger log = Logger.getLogger(Loggin.class);
	
	/*public static void main(String[] args) {
		System.out.println("this is a normal print");
		log.trace("trace message: very fine grained");
		log.debug("This statement would be useful for debugging application");
		log.info("information message use this level of loggin");
		log.warn("something bad potentiall could be happening");
		log.error("Something went wrong");
		log.fatal("something extremely wrong happened");
	}*/
}
